{
  "enabled": {
    "description": "启用自动预览",
    "type": "bool",
    "default": true
  },
  "group_allowlist": {
    "description": "允许的群号列表",
    "type": "list",
    "default": [],
    "hint": "留空表示所有群；填写后只处理列表内的群号。私聊不受此列表限制。"
  },
  "proxy_url": {
    "description": "HTTP/HTTPS 代理",
    "type": "string",
    "default": "",
    "hint": "例如 http://172.19.0.1:7890。留空表示不使用代理；不要在此填写带密码的临时代理。"
  },
  "max_links_per_message": {
    "description": "单条消息最多处理的链接数",
    "type": "int",
    "default": 2
  },
  "authenticated_enabled": {
    "description": "启用绑定 QQ 的只读登录通道",
    "type": "bool",
    "default": false,
    "hint": "只有下方 QQ sender allowlist 中的发送者可触发；群聊还必须单独开启测试开关。"
  },
  "authenticated_sender_allowlist": {
    "description": "允许登录抓取的 QQ sender_id",
    "type": "list",
    "default": [],
    "hint": "必须显式填写；留空时即使启用登录通道也不会向任何 QQ 会话返回受限正文。"
  },
  "authenticated_allow_group_messages": {
    "description": "允许绑定 QQ 在群聊触发登录抓取",
    "type": "bool",
    "default": false,
    "hint": "仅 allowlist 中的发送者可触发；正文会对所在群可见。开发测试后应关闭。"
  },
  "authenticated_secret_file": {
    "description": "认证侧车 secret 文件",
    "type": "string",
    "default": "/AstrBot/data/secrets/astrbot_plugin_linuxdo_preview.json",
    "hint": "支持只读 User API v1 或隔离浏览器会话 v2；只填写绝对路径，内容不得写入本配置、Git 或日志，权限必须为 0600。"
  },
  "reply_on_error": {
    "description": "抓取失败时在当前会话提示",
    "type": "bool",
    "default": true
  }
}
