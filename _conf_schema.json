{
  "enabled": {
    "description": "启用自动预览",
    "type": "bool",
    "default": true
  },
  "group_allowlist": {
    "description": "允许的群号列表",
    "type": "list",
    "default": [],
    "hint": "留空表示所有群；填写后只处理列表内的群号。私聊不受此列表限制。"
  },
  "proxy_url": {
    "description": "HTTP/HTTPS 代理",
    "type": "string",
    "default": "",
    "hint": "例如 http://172.19.0.1:7890。留空表示不使用代理；不要在此填写带密码的临时代理。"
  },
  "max_links_per_message": {
    "description": "单条消息最多处理的链接数",
    "type": "int",
    "default": 2
  },
  "cache_ttl_seconds": {
    "description": "成功预览缓存秒数",
    "type": "int",
    "default": 1800
  },
  "dedup_ttl_seconds": {
    "description": "同会话同帖去重秒数",
    "type": "int",
    "default": 300
  },
  "reader_timeout_seconds": {
    "description": "Reader 主题 HTML 请求超时秒数",
    "type": "int",
    "default": 45,
    "hint": "Reader 只提取公开主题的 #post_1 .cooked 节点，不会接收 Cookie 或登录材料。"
  },
  "reader_requests_per_minute": {
    "description": "reader 每分钟最大请求数",
    "type": "int",
    "default": 12,
    "hint": "公开通道实测窗口为 20 RPM；默认留出余量，超限时快速失败。"
  },
  "max_content_chars": {
    "description": "单张长图最多保留的正文字符数",
    "type": "int",
    "default": 12000
  },
  "image_quality": {
    "description": "长图 JPEG 质量",
    "type": "int",
    "default": 88,
    "hint": "范围 60-95；质量越高，QQ 上传文件越大。"
  },
  "max_images_per_topic": {
    "description": "单个帖子最多加载的图片数",
    "type": "int",
    "default": 6,
    "hint": "范围 0-12；设为 0 时不下载帖子图片，只显示数量提示。"
  },
  "max_image_bytes": {
    "description": "单张图片最大字节数",
    "type": "int",
    "default": 2000000,
    "hint": "同时限制下载体积和处理后的 JPEG 体积，范围 128000-8000000。"
  },
  "max_total_image_bytes": {
    "description": "单帖图片处理后最大总字节数",
    "type": "int",
    "default": 6000000,
    "hint": "范围 256000-16000000；达到上限后的图片保留占位。"
  },
  "image_timeout_seconds": {
    "description": "单张图片下载超时秒数",
    "type": "int",
    "default": 15,
    "hint": "范围 5-45；图片失败不会影响正文长图。"
  },
  "render_timeout_seconds": {
    "description": "整张长图渲染超时秒数",
    "type": "int",
    "default": 90,
    "hint": "范围 30-180；避免外部 T2I 不响应时长期占用消息处理。"
  },
  "reply_on_error": {
    "description": "抓取失败时在当前会话提示",
    "type": "bool",
    "default": true
  }
}
